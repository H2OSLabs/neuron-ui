{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NRadioGroupProps",
  "description": "Props for the NRadioGroup component. A group of mutually exclusive radio buttons for selecting exactly one option.",
  "type": "object",
  "properties": {
    "options": {
      "type": "array",
      "description": "List of radio options to display",
      "items": {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "description": "The option value used for form submission"
          },
          "label": {
            "type": "string",
            "description": "The display label shown next to the radio button"
          },
          "disabled": {
            "type": "boolean",
            "description": "Whether this individual radio option is disabled"
          }
        },
        "required": ["value", "label"],
        "additionalProperties": false
      }
    },
    "value": {
      "type": "string",
      "description": "The currently selected radio value"
    },
    "className": {
      "type": "string",
      "description": "Additional CSS class names for custom styling"
    }
  },
  "required": ["options"],
  "additionalProperties": false
}
