{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NResizableProps",
  "description": "Neuron resizable panel container for creating adjustable split-pane layouts. Supports horizontal and vertical split directions.",
  "type": "object",
  "properties": {
    "direction": {
      "type": "string",
      "enum": ["horizontal", "vertical"],
      "default": "horizontal",
      "description": "Direction of the resizable split: 'horizontal' creates left/right panels, 'vertical' creates top/bottom panels."
    },
    "className": {
      "type": "string",
      "description": "Additional CSS class names to apply."
    }
  },
  "required": [],
  "additionalProperties": false
}
