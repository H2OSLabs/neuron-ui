{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NNavigationMenuProps",
  "description": "Neuron navigation menu component for site-level navigation with links and optional dropdowns. Designed for primary navigation bars in application layouts.",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "description": "List of navigation items to render.",
      "items": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string",
            "description": "Display label for the navigation item."
          },
          "href": {
            "type": "string",
            "description": "URL or route path the navigation item links to."
          }
        },
        "required": ["label"],
        "additionalProperties": false
      }
    },
    "className": {
      "type": "string",
      "description": "Additional CSS class names to apply."
    }
  },
  "required": ["items"],
  "additionalProperties": false
}
